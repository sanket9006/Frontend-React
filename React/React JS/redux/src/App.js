import './App.css';
// import redux from './Components/redux_plain_javascript'
import redux from './Components/redux'

function App() {
  <script src={redux} />

  return (
    <div className="App">
      It's an simple example which shows how to use redux as standalone [ using simple JS ]
      <br />
      Have a look at console
    </div>
  );
}

export default App;
